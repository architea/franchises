---
const { src, class: classList } = Astro.props
let innerHTML = await fetch(src).then((res) => res.text())
---

<div class=`${classList ? classList : ''}`>
  <Fragment set:html={innerHTML} />
</div>
